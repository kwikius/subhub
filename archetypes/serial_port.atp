
archetype apm::GPSSerialPort {

   size_t write(uint8_t c);
   std::size_t write(uint8_t* data, std::size_t datalength);
   std::size_t available();
   size_t read();
   size_t tx_space();
   begin(baudrate);
   set_flow_control(SerialPort::flow_control_type );
}