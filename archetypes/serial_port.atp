----------------------------
apm::GPSSerialPort archetype
----------------------------

archetype apm::GPSSerialPort {

   typedef size_type;
   size_type write(uint8_t c);
   size_type write(uint8_t* data, std::size_t datalength);
   size_type available();
   size_type read();
   size_type tx_space();
   void begin(baudrate);
}